export interface Paths {
  LOGIN: string;
  REGISTRATION: string;
  ROOT: string;
  HOME: string;
}

export const paths: Paths = {
  LOGIN: '/login',
  REGISTRATION: '/registration',
  ROOT: '/',
  HOME: '/home'
};

